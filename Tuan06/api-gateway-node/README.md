# API Gateway

Node.js API Gateway for the Mini Food Ordering system.

## Routes

- `/register`, `/login`, `/users` -> User Service
- `/foods` -> Food Service
- `/orders` -> Order Service
- `/payments` -> Payment Service

## Run

```bash
npm install
copy .env.example .env
npm run dev
```

Default port: `8080`
